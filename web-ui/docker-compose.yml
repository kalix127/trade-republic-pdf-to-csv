services:
  python-api:
    build:
      context: ./python-api
      dockerfile: Dockerfile
    container_name: tr-python-api
    environment:
      - ENVIRONMENT=production
      - PORT=8000
    ports:
      - "8001:8000"
    networks:
      - tr-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 30s
      retries: 5
      start_period: 120s
  web-ui:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: tr-web-ui
    environment:
      - NODE_ENV=production
      - NITRO_HOST=0.0.0.0
      - NITRO_PORT=3001
      - PYTHON_API_URL=http://python-api:8000
    ports:
      - "3001:3001"
    networks:
      - tr-network
    depends_on:
      python-api:
        condition: service_healthy
    restart: unless-stopped

networks:
  tr-network:
    driver: bridge
    name: trade-republic-network
